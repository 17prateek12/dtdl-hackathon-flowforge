// Tailwind v3 + autoprefixer — pure JavaScript, no native binary.
// Works with both Turbopack (Next.js 16) and webpack without crashing.
const config = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};

export default config;
